// Moved: this verifier lives in apps/web/scripts/verify_ui_batch.mjs
// (playwright resolves only from apps/web in the pnpm workspace).
// Run:  cd apps/web && node scripts/verify_ui_batch.mjs
console.error('Run from apps/web instead: cd apps/web && node scripts/verify_ui_batch.mjs')
process.exit(1)
