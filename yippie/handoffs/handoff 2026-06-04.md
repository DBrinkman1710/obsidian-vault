# Handoff — 2026-06-04

**Branch:** `claude/modular-account-management-design-XrQwj`  
**Repo:** github.com/DBrinkman1710/obsidian-vault  
**Session:** Infrastructure fixes — all environments healthy

---

## What was done

### 1. Diagnosed and fixed Railway deploy failures

Every environment had been in "Deploy failed" for 9+ hours. Four separate root causes were found and fixed:

| Commit | Fix |
|---|---|
| `2212858` | Restored `resolve_tenant_uuid` in `app/core/tenant.py` — was deleted in the multi-tenant refactor but `inbox/router.py` still imported it, crashing uvicorn on startup |
| `29646f9` | Added public `GET /api/v1/health` endpoint and pointed Railway healthcheck there — old endpoint `/api/v1/tenant/config` now requires a JWT, so the unauthenticated healthcheck was getting 401; also created merge migration `08f0431f4fec` to unify two divergent alembic branches and changed start command to `alembic upgrade heads` |
| `21fd5fe` | Added `email-validator` to `pydantic[email]` in `requirements.txt` — admin module uses `EmailStr` which requires it |

### 2. Applied missing DB migrations to production

Two migration branches had diverged from `40bd1ebcda20`:
- Branch A (applied): `ca4c445f94a2 → a1b2c3d4e5f6 → b2c3d4e5f6a7 → c3d4e5f6a7b8`
- Branch B (missing): `e1f2a3b4c5d6` (superadmin enum) `→ f2e3d4c5b6a7` (tenant config columns)

Ran `alembic upgrade f2e3d4c5b6a7` on production manually before the deploy fix landed. Dev and sandbox applied all migrations automatically via `alembic upgrade heads` on their next successful deploy.

### 3. Promoted superadmin on production

Ran `promote_superadmin.py` against production — upgraded `diederik1710@gmail.com` from `admin` → `superadmin`. Dev and sandbox were already seeded with `superadmin` role by `seed.py`.

### 4. Set superadmin password

Generated a new 24-character password and updated the bcrypt hash directly in all three DBs (production, development, sandbox). Password was saved to `~/Desktop/yippie-superadmin-password.txt` for the user to write down and delete.

### 5. Fixed getyippie.com 502

| Commit | Fix |
|---|---|
| `75828b3` | `HOSTNAME=0.0.0.0` in `apps/web/railway.json` start command — Next.js standalone server was binding to `localhost` so Railway's edge proxy couldn't reach it |

`getyippie.com` (Next.js marketing site) now returns 200. No Cloudflare changes were needed — the issue was entirely on the Railway side.

### 6. Sandbox environment

The Sandbox Railway environment already existed (forked from production) with Postgres and `sandbox.getyippie.com` configured. It was failing for the same reasons as production. Fixed by fast-forwarding the `sandbox` branch with all commits from `claude/modular-account-management-design-XrQwj`:

```bash
git checkout sandbox
git merge origin/claude/modular-account-management-design-XrQwj --ff-only
git push origin sandbox
```

---

## State of all environments

| Environment | URL | Status | DB |
|---|---|---|---|
| Production | app.getyippie.com | ✅ Online | Migrations at head, superadmin set |
| Development | dev.getyippie.com | ✅ Online | Migrations at head, superadmin set |
| Commercial | getyippie.com | ✅ Online | Next.js only, no DB |
| Sandbox | sandbox.getyippie.com | ✅ Online | Migrations at head, superadmin set |

**Superadmin account:** `diederik1710@gmail.com` — same password across all three app environments.

---

## Migration chain (final state)

```
bfadac4990f3  initial schema
ca081fe1f74d  add matched_contact_id + context fields
f43013171b86  add context fields to draft_tickets
40bd1ebcda20  add follow_up_at to draft_tickets          ← branch point
├─ ca4c445f94a2  add departments table
│  └─ a1b2c3d4e5f6  add department_id to tickets
│     └─ b2c3d4e5f6a7  add detected_language to draft_tickets
│        └─ c3d4e5f6a7b8  enable RLS tenant isolation
└─ e1f2a3b4c5d6  add superadmin role to userrole enum
   └─ f2e3d4c5b6a7  add enabled_modules, primary_color, logo_url to tenants
08f0431f4fec  merge branches (HEAD)
```

---

## What's still outstanding

- **`ADMIN_PASSWORD` Railway env var** — Should be updated in Railway dashboard (production + dev + sandbox) to match the new password. Currently the env var still says `password`, but the DB hash is correct. Only matters if the DB is wiped and re-seeded.
- **Superadmin UI** — No React frontend for client management yet. Backend API is complete (`POST /api/v1/admin/tenants`, `PATCH /api/v1/admin/tenants/{id}`, etc.).
- **Webhook routing for multi-tenant** — `resolve_tenant_uuid` is a stub that returns the first tenant. Works for single-tenant but needs per-tenant webhook URLs (`/api/v1/inbox/webhooks/email/{tenant_slug}`) before onboarding a second client.
- **PostgreSQL RLS policies** — Migration scaffolded, policies not yet written.
- **Onboard first test client** — See below.

---

## How to continue

### Verify login works
```
POST https://app.getyippie.com/api/v1/auth/token
  email: diederik1710@gmail.com
  password: <from the file you saved>
```

### Onboard a test client
```bash
curl -X POST https://app.getyippie.com/api/v1/admin/tenants \
  -H "Authorization: Bearer <superadmin-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test Client",
    "slug": "testclient",
    "admin_email": "client@test.com",
    "admin_password": "pass123",
    "enabled_modules": ["contacts", "tickets"]
  }'
```

### Next feature: Superadmin UI
Build a React admin panel at `/admin` (route gated on `superadmin` role) to manage clients visually. The backend API at `/api/v1/admin/tenants` is complete.
