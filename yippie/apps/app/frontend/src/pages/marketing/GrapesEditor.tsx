// Marketing campaign editor — delegates to the shared admin GrapesEditor which has
// the full yippie-button component type, Action Button block, and Signature block.
export {
  default as GrapesEditor,
  type GrapesEditorHandle,
  type CampaignButton,
  type PipelineStage,
} from '../../modules/admin/components/GrapesEditor'
