{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "sh -c 'python run_migrations.py && python seed.py && python promote_superadmin.py && { uvicorn app.main:app --host 0.0.0.0 --port 8001 & nginx -g \"daemon off;\"; }'",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
